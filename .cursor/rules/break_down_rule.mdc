# Task Breakdown Rules

You are an expert project manager and software architect. Given a technical design document, your task is to break it down into a comprehensive, actionable checklist of smaller tasks. This checklist should be suitable for assigning to developers and tracking progress.

## Input

You will receive a Markdown document representing the technical design of a feature or component. This document will follow the structure outlined in the "Documentation Style" section above (Overview, Purpose, Design, Dependencies, Usage, Error Handling, Open Questions).

## Output

Generate a Markdown checklist representing the task breakdown.

## Guidelines

1.  **Granularity:** Tasks should be small enough to be completed within a reasonable timeframe (ideally a few hours to a day). Avoid tasks that are too large or too vague.
2.  **Actionable:** Each task should describe a specific, concrete action that a developer can take. Use verbs like "Create", "Implement", "Add", "Update", "Refactor", "Test", "Document", etc.
3.  **Dependencies:** Identify any dependencies between tasks. If task B depends on task A, make this clear (either through ordering or explicit notes).
4.  **Completeness:** The checklist should cover all aspects of the technical design, including:
    - Database schema changes (migrations).
    - API endpoint creation/modification.
    - UI changes (if applicable).
    - Business logic implementation.
    - Unit test creation.
    - Integration test creation (if applicable).
    - Documentation updates.
    - Addressing any open questions.
5.  **Clarity:** Use clear and concise language. Avoid jargon or ambiguity.
6.  **Checklist Format:** Use Markdown's checklist syntax:
    ```
    - [ ] Task 1: Description of task 1
    - [ ] Task 2: Description of task 2
    - [ ] Task 3: Description of task 3 (depends on Task 2)
    ```
7.  **Categorization (Optional):** If the feature is large, consider grouping tasks into categories (e.g., "Database", "API", "Testing").
8.  **Prioritization (Optional):** If some tasks are higher priority than others, indicate this (e.g., using "(High Priority)" or a similar marker).

## Example

**Input (Technical Design Document - Excerpt):**

````markdown
## CreateCategoryCommand

**Overview:** This command creates a new Category.

**Purpose:** Allows administrators to define new categories for organizing items.

**Design:**

- Takes a `CreateCategoryCommand` as input.
- Uses repository pattern to interact with the database.
- Checks for existing categories with the same name.
- Creates a new `Category` aggregate via factory.
- Saves changes using the repository.

**Dependencies:**

- `CategoryRepositoryPort`

**Usage:**

```typescript
// Example usage
const command = new CreateCategoryCommand({
  name: 'MyCategory',
  description: 'Description of my category',
});
const result = await this.commandBus.execute(command);
```
````

**Error Handling:**

- Returns the created `Category` entity or throws an exception.
- If a category with the same name already exists, throws `ConflictException`.
- Uses `class-validator` decorators in the command DTO to ensure the command is valid.

**Open Questions:**

- None

````

**Output (Task Breakdown):**

```markdown
- [ ] Task 1: Create `CreateCategoryCommand` class in `src/categories/application/commands/create-category.command.ts`.
- [ ] Task 2: Create `CreateCategoryDto` class with validation decorators.
- [ ] Task 3: Implement `CreateCategoryHandler` class.
    - [ ] Inject `CategoryRepositoryPort` via DI token.
    - [ ] Implement `execute` method:
        - [ ] Check for existing category with the same name using repository.
        - [ ] Create a new `Category` aggregate using factory.
        - [ ] Save the category using repository.
        - [ ] Return the created category.
- [ ] Task 4: Create unit tests for `CreateCategoryHandler`.
    - [ ] Test successful category creation.
    - [ ] Test case where category with the same name already exists.
    - [ ] Test validation failures.
- [ ] Task 5: Update documentation for `CreateCategoryCommand` and `CreateCategoryHandler`.
```

**Another Example (with dependencies and categories):**

**Input (Technical Design Document - Excerpt - for a hypothetical "Update Product" feature):**

```markdown
## UpdateProduct Command

**Overview:** Allows updating the name, description, and price of a Product.

**Design:**

- Takes an `UpdateProductCommand` (with `Id`, `Name`, `Description`, `Price`).
- Retrieves the existing `Product` from the repository.
- Calls the `update()` method on the domain entity.
- Saves changes using the repository.
- Needs a new migration to add a `description` column.

**Dependencies:**

- `ProductRepositoryPort`

... (rest of the document) ...
```

**Output (Task Breakdown):**

```markdown
**Database:**

- [ ] Task 1: Create a new database migration to add the `description` column to the `products` table. (High Priority)

**Domain Layer:**

- [ ] Task 2: Add `update()` method to `Product` aggregate if not present.

**Application Layer:**

- [ ] Task 3: Create `UpdateProductCommand` class in `src/products/application/commands/update-product.command.ts`.
- [ ] Task 4: Create `UpdateProductDto` class with validation decorators.
- [ ] Task 5: Implement `UpdateProductHandler` class.
  - [ ] Inject `ProductRepositoryPort` via DI token.
  - [ ] Implement `execute` method:
    - [ ] Retrieve existing `Product` by ID using repository.
    - [ ] Call `update()` method on the domain entity.
    - [ ] Save changes using repository.
    - [ ] Return the updated product.

**Testing:**

- [ ] Task 6: Write unit tests for `UpdateProductHandler`.
  - [ ] Test successful update.
  - [ ] Test case where `Product` is not found.
  - [ ] Test validation failures.

**Documentation:**

- [ ] Task 7: Update documentation for `Product` and the new command/handler.
```
````
